{
  "name": "studentgrievancecell",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node server.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npx vite build",
    "start": "node server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
